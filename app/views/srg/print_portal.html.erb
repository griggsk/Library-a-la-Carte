	<h1><%=@local.guide_page_title %> </h1>
			<h2>Browse Subject Research Guides</h2>
			
		<div class="list">
				  <ul class = 'indent-left'>
			   	 <% for guide in @guides %>
					        <%guide_url = url_for :controller => 'srg', :action => 'index', :id => guide%>
					            <li class = 'undeco'><%= link_to guide.guide_name, guide_url, :title => "#{h guide.description}" %> </li>
				 <%end%>
				 </ul>
		</div>
		   <p> Need help with your course assignment? Try the <%= link_to "Course Guides", :controller => 'ica', :action =>'published_pages' %> </p>
		
		  <h2>Browse by Tags</h2>
		  <div class="list">
		   <ul class = 'indent-left'>
			<% tag_cloud @tags, %w(css1 css2 css3 css4) do |tag, css_class| %>
   			     <li><%= link_to tag.name, { :action => :tagged, :id => tag.name }, :class => css_class %></li>
 		   <% end %>	
		   </ul>
		  </div>
 <%= if !@local.footer.blank? then @local.footer end %>
 
 <p><%= link_to "Go To Full Version" , {:action => 'published_guides'}%></p>